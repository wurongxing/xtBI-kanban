# 小铁台球教培经营看板数据口径与同步说明

更新时间：2026-07-31  
适用版本：经营看板修复版_明细自动汇总

## 1. 看板定位

本看板从“OKR 导航舱”升级为“经营导航舱”，当前重点展示：

- 公司总目标、公司总完成、本月时间进度、周目标、周完成率。
- 深圳经营中心、广州经营中心的城市经营数据。
- 城市业绩、用户端、教练端、门店端月度与昨日数据。
- 城市地图/城区分布：按区域展示教练数、教练姓名、门店数、门店名称。
- 教练经营排名：支持按字段排序、Excel 式筛选、全选、全不选、确认。
- 总部运营中心 OKR：部门 OKR、项目 OKR、总部运营提醒、流量到转化漏斗。
- 项目进度：六项目 OKR、目标值、完成值、KR、风险与具体动作。
- 导出：当前页面 PNG、PDF 报告、Excel 模板下载、本地 Excel 上传。

品牌表达：

- 愿景：打造最靠谱的智能台球教育平台。
- 使命：科技赋能、学员得技、教练得益、球房得利。
- 价值观：践行、精进、共赢、致远。
- 主色：克莱因蓝；辅助色：小铁黄。

## 2. 数据源优先级

当前版本支持四类数据源，优先级如下：

1. 本地上传 Excel：用户在看板页面上传 Excel 后，当前浏览器立即使用上传数据。
2. 本地上传 JSON：用户从后台导出订单、教练、门店 JSON 后，可直接上传一个或多个 JSON 文件，系统自动转成经营仓明细并汇总。
3. 钉钉表格实时同步：线上部署后，请求 Vercel `/api/dingtalk-data`，由服务端读取钉钉共享表格。
4. 静态示例数据：当同步失败或本地 file 打开时，使用内置 `data.json` 或示例数据兜底。

前端自动同步规则：

- 线上访问时默认读取 `/api/dingtalk-data`。
- 本地 file 打开时默认读取 `./data.json`。
- 自动同步间隔：30 秒。
- 点击“刷新数据”会立即触发一次同步。
- 若设置了自定义同步接口，则优先读取用户输入的接口地址。

推荐操作方式：

- 临时方案：后台每天导出门店 JSON、教练 JSON、订单 JSON，在看板“数据中心”点击“上传JSON数据”，选择多个 JSON 文件一次导入。
- 长期方案：后台提供只读 API token，Vercel 直接请求后台 API，做到真正免人工实时同步。
- 不推荐长期使用“JSON 转 Excel -> 复制到钉钉 -> 看板同步钉钉”，这条链路人工步骤多，容易出现字段错位、复制漏行、日期格式被改等问题。

## 3. 钉钉表格同步配置

Vercel 环境变量需要填写：

```env
DINGTALK_SYNC=true
DINGTALK_APP_KEY=钉钉应用AppKey
DINGTALK_APP_SECRET=钉钉应用AppSecret
DINGTALK_WORKBOOK_ID=钉钉表格WorkbookId
DINGTALK_OPERATOR_ID=操作者OperatorId
DINGTALK_SHEETS={"基础配置":"sheetId","双城经营":"sheetId","公司KR":"sheetId","六部门OKR":"sheetId","六项目OKR":"sheetId","门店明细":"sheetId","教练档案":"sheetId","教练门店关系":"sheetId","体验课流水":"sheetId","续课流水":"sheetId","教练经营":"sheetId","城区分布":"sheetId","转化漏斗":"sheetId","经营重点":"sheetId"}
DINGTALK_RANGE=A1:Z500
DINGTALK_DETAIL_RANGE=A1:Z2000
DINGTALK_CONCURRENCY=4
DINGTALK_HTTP_TIMEOUT_MS=9000
```

说明：

- `DINGTALK_APP_SECRET` 只能放在 Vercel 环境变量里，不能写进前端代码。
- `DINGTALK_SHEETS` 必须是合法 JSON，中文 sheet 名要和模板一致。
- 明细类 sheet 数据量较大，默认读取 `A1:Z2000`。
- 普通配置类 sheet 默认读取 `A1:Z500`。
- 若钉钉 504 或超时，可减少 `DINGTALK_REQUIRED_SHEETS` 或调低并发。

## 3.1 后台 JSON 导入口径

看板支持直接上传后台导出的 JSON 文件，适合当前后台只能导出 JSON 的阶段。

支持方式：

- 可一次选择多个 JSON 文件。
- 支持门店、教练、订单、续课、教练门店关系等 JSON。
- 如果上传的是已经处理好的看板 JSON，系统会直接展示。
- 如果上传的是后台原始 JSON，系统会自动识别类型并转换为经营仓明细。
- 上传后台原始 JSON 时，只刷新经营明细汇总；城市目标、公司 KR、部门 OKR、项目 OKR、转化漏斗会沿用当前看板配置。

自动识别规则：

- 文件名或字段含 `门店`、`store`、`site`、`site_name`、`门店名称`：识别为门店明细。
- 文件名或字段含 `教练`、`coach`、`coach_id`、`教练等级`：识别为教练档案。
- 文件名或字段含 `教练门店`、`关联`，同时含教练 ID 和门店 ID/名称：识别为教练门店关系。
- 文件名或字段含 `续课`、`续约`、`renew`：识别为续课流水。
- 其他默认识别为订单/体验课流水。

JSON 转经营仓字段：

- 门店 JSON -> `门店明细`
  - 门店 ID、城市、区域、门店名称、地址、关联教练数、门店状态、门店类型、入驻类型、是否付费、入驻日期。
  - 后台门店列表格式中，`id` 映射为门店 ID，`name` 映射为门店名称，`city` 映射为城市，`district` 映射为区域，`address` 映射为地址，`coach_count` 映射为关联教练数，`created_at` 映射为入驻日期/新增时间。
  - `status` 映射：1 营业中，2 暂停营业，3 闭店，4 装修中，5 删除，6 测试。
  - `show_status` 映射：1 上架，2 下架。
  - `source` 映射：1 其他门店，2 小铁门店。
  - `room_type` 映射：1 自助球房，2 商务球房，3 竞技球房。
  - `is_value_added=true` 映射为付费入驻，`false` 映射为免费入驻；`has_onsite_coach` 映射为驻场教练。
  - `入驻日期` 在本月时自动标记本月新增。
  - `入驻日期` 在昨日时自动标记昨日新增。
- 教练 JSON -> `教练档案`
  - 教练 ID、城市、区域、教练、教练等级、在职状态、入职日期、服务门店。
  - 后台教练列表格式中，`id` 映射为教练 ID，`name/show_name` 映射为教练，`belong_city` 映射为城市，`residence_area` 映射为区域，`level` 映射为 L1/L2/L3，`status=1` 映射为在职，`created_at` 映射为入职日期/新增时间。
  - 一个教练有多个 `residence_area` 时，教练档案仍保持一人一行；城区分布统计时会按多个区分别计入，教练排名不会重复出现。
  - `入职日期` 在本月时自动标记本月新增。
  - `入职日期` 在昨日时自动标记昨日新增。
- 订单 JSON -> `体验课流水`
  - 日期、订单 ID、城市、区域、教练 ID、教练、门店 ID、门店名称、课程名称、下单数、转化数、已消课数、金额、渠道、状态。
  - 状态含退款、已退、取消时，下单数和金额按 0 处理。
  - 状态或字段显示已转化、成交、转化时，转化数按 1 处理。
  - 课程名称含续课、续费、续约、正课时，会转入续课流水。

JSON 导入后的时间口径：

- 本月数据：系统自动识别当前自然月；门店按 `入驻日期`，教练按 `入职日期`，订单按订单日期/下单时间/支付时间。
- 昨日数据：系统自动识别当前日期的前一天；门店、教练、订单均按对应日期字段过滤。
- 本周数据：最近 7 天。

## 4. Excel/钉钉 Sheet 名称

最新模板包含以下 sheet：

- 模板说明
- 字段字典
- 基础配置
- 双城经营
- 公司KR
- 六部门OKR
- 六项目OKR
- 门店明细
- 教练档案
- 教练门店关系
- 体验课流水
- 续课流水
- 教练经营
- 城区分布
- 转化漏斗
- 经营重点

注意：

- Sheet 名称使用中文，便于运营团队维护。
- 看板读取时按表头匹配字段，不按列号固定匹配。
- 第一行可以是说明文字，但必须存在一行完整表头。
- 表头不要随意改名；需要新增字段时，应优先在末尾新增。

## 5. 基础配置口径

基础配置用于定义公司级信息和周期。

常用字段：

- `company`：公司名称，默认“小铁台球教培”。
- `period`：统计周期，例如“2026年7月”。
- `totalGoal`：公司经营目标，单位万元。

时间口径：

- 本月：`period` 所在自然月。
- 本周：当前日期往前 6 天到今天，合计 7 天。
- 昨日：当前日期前一天 00:00:00 到 23:59:59。
- 更新时间：服务端返回数据时按 Asia/Shanghai 生成。

## 6. 城市经营中心口径

城市经营中心按城市展示深圳、广州。

业绩字段：

- 月度目标：来自 `双城经营.月度目标_万元`，没有时使用 `目标营收_万元`。
- 月度完成：优先由明细自动汇总，公式为 `体验课流水.金额 + 续课流水.金额`，单位换算为万元。
- 完成率：`月度完成 / 月度目标 * 100%`。
- 周目标：来自 `双城经营.周目标_万元`，没有时按 `月度目标 / 4` 兜底。
- 周完成：最近 7 天 `体验课流水.金额 + 续课流水.金额`。
- 昨日完成：昨日 `体验课流水.金额 + 续课流水.金额`。

用户端、教练端、门店端：

- 月度体验课数：本月 `体验课流水.下单数` 求和。
- 月度成交数：本月 `体验课流水.转化数` 求和。
- 月度转化率：`月度成交数 / 月度体验课数 * 100%`。
- 月度续课数：本月 `续课流水.续约数` 求和；若没有续约数字段，则按续课流水行数兜底。
- 昨日体验课数：昨日 `体验课流水.下单数` 求和。
- 昨日成交数：昨日 `体验课流水.转化数` 求和。
- 昨日转化率：`昨日成交数 / 昨日体验课数 * 100%`。
- 昨日续课数：昨日 `续课流水.续约数` 求和；若没有续约数字段，则按续课流水行数兜底。

门店端入驻口径：

- 入驻门店数：该城市 `门店明细` 总行数。
- 付费入驻门店数：`入驻类型` 包含“付费”，或 `是否付费` 大于 0。
- 免费入驻门店数：入驻门店数 - 付费入驻门店数。
- 本月新签门店数：`入驻日期` 在本月，或 `新增类型` 有值。
- 本月新签付费门店数：本月新签门店中符合付费口径的数量。
- 本月新签免费门店数：本月新签门店中不符合付费口径的数量。
- 昨日新增门店数：`入驻日期` 在昨日，或 `昨日新增` 为真。
- 新增门店展示：显示所在区和门店名称。

教练端新增口径：

- 总教练数：该城市 `教练档案` 中 `在职状态` 不等于“离职”的教练数。
- 本月新增教练数：`入职日期` 在本月，或 `新增类型` 有值。
- 昨日新增教练数：`入职日期` 在昨日，或 `昨日新增` 为真。
- 新增教练展示：显示新增教练姓名。

## 7. 城区分布口径

城区分布按 `城市 + 区域` 聚合。

教练：

- 来源：`教练档案`。
- 匹配字段：`城市`、`区域`。
- 展示：教练数、教练姓名列表。

门店：

- 来源：`门店明细`。
- 匹配字段：`城市`、`区域`。
- 展示：门店数、门店名称列表。

一个教练服务多个门店：

- 来源：`教练门店关系`。
- 匹配字段：`教练ID`。
- 聚合方式：同一教练 ID 下的多个 `门店名称` 合并展示。

## 8. 教练经营排名口径

教练经营排名每行一个教练，字段分列展示：

- 教练
- 等级
- 区域
- 服务门店
- 累计体验课
- 累计转化
- 累计转化率
- 月度体验课
- 月度转化
- 月度转化率
- 昨日体验课
- 昨日成交
- 昨日转化率
- 用户数
- 续课数
- 续客率

自动汇总逻辑：

- 教练匹配：优先用 `教练ID`，没有时用 `教练` 姓名。
- 累计体验课：该教练全部 `体验课流水.下单数` 求和。
- 累计转化：该教练全部 `体验课流水.转化数` 求和。
- 累计转化率：`累计转化 / 累计体验课 * 100%`。
- 月度体验课：本月该教练 `体验课流水.下单数` 求和。
- 月度转化：本月该教练 `体验课流水.转化数` 求和。
- 月度转化率：`月度转化 / 月度体验课 * 100%`。
- 昨日体验课：昨日该教练 `体验课流水.下单数` 求和。
- 昨日成交：昨日该教练 `体验课流水.转化数` 求和。
- 昨日转化率：`昨日成交 / 昨日体验课 * 100%`。
- 用户数：当前按累计转化数作为正课用户兜底口径。
- 续课数：该教练全部 `续课流水.续约数` 求和；没有续约数字段时按续课流水行数兜底。
- 续客率：`续课数 / 用户数 * 100%`。

筛选和排序：

- 表头支持排序。
- 表头支持 Excel 式筛选：搜索、全选、全不选、确认。
- 筛选作用于当前城市教练排名。

## 9. OKR 口径

公司 KR：

- 来源：`公司KR`。
- 按 `period_type` 区分 `month`、`week`、`day`。
- 展示字段：KR 编号、KR 名称、目标、完成、完成率、负责人、支持部门、风险、关键行动。

部门 OKR：

- 来源：`六部门OKR`。
- 展示字段：部门、Objective、负责人、目标值、实际完成、单位、完成率、风险/卡点、下一步具体动作、截止日期、关键 KR。

项目 OKR：

- 来源：`六项目OKR`。
- 展示字段：项目、Objective、负责人、目标值、实际完成、单位、完成率、状态、风险/卡点、下一步具体动作、关键 KR。

个人 OKR：

- 当前首页不展示个人 OKR。
- 代码仍保留个人 OKR 数据结构，可作为后续恢复或独立页面使用。
- 个人为一个 Objective 对应多条 KR，按姓名聚合后计算平均完成率。

## 10. 总部运营中心漏斗口径

来源：`转化漏斗`。

漏斗层级：

1. 小程序日活；副标题：浏览转体验转化率。
2. 体验课下单数；副标题：退款数；渠道：美团、抖音、私域、其他。
3. 未退款数；副标题：下单未预约数、预约未上课数。
4. 已消课数；副标题：有效下单消课率。
5. 转化数；副标题：转化率。
6. 续约数；副标题：续约率。

展示方式：

- 按 `排序` 字段从小到大展示。
- 主内容取 `环节` 和 `数值`。
- 副标题取 `副标题`，没有时取 `说明`、`备注` 或 `辅助指标`。
- 渠道值读取 `美团`、`抖音`、`私域`、`其他`。

## 11. 经营重点口径

来源：`经营重点`。

用于展示：

- 风险预警
- 亮点分析
- 经营建议
- 总部运营提醒

展示位置：

- 城市经营中心：风险预警、亮点分析、经营建议放在页面底部。
- 总部运营中心：总部运营提醒放在最下面一列。

## 12. 后台 API 直连预留方案

后台已确认的 API 基础地址：

```text
https://table-coach.xtpool.com/api/admin/v1
```

后台请求头：

```text
Authorization: Bearer <后台专用token>
```

已确认或可用的接口路径：

- 门店列表：`GET /sites/list`
- 门店详情：`GET /sites/{id}`
- 门店关联教练：`GET /sites/{id}/coaches`
- 教练列表：`GET /coaches/list`
- 教练详情：`GET /coaches/{id}`
- 教练课程：`GET /coaches/{id}/courses`
- 课程订单：`GET /orders/list`
- 预约课程：`GET /orders/appointment/list`
- 订单详情：`GET /orders/{id}`
- 数据导出类型：`GET /data/exports/types`
- 创建数据导出：`POST /data/exports`
- 数据导出列表：`GET /data/exports/list`
- 下载数据导出：`POST /data/exports/{id}/download`

若切换到后台直连，Vercel 需要新增：

```env
XT_BACKEND_SYNC=true
XT_BACKEND_BASE_URL=https://table-coach.xtpool.com/api/admin/v1
XT_BACKEND_TOKEN=后台专用只读token
```

安全要求：

- 不从浏览器 cookie、localStorage 或登录态中提取 token。
- 建议由后台创建专用只读 token 或专用只读账号。
- Token 只放 Vercel 环境变量，不写入前端代码。

## 13. 数据缺失和兜底规则

- 数字为空：按 0 处理。
- 百分号字段：支持 `28.5` 或 `28.5%`，读取时会去掉 `%`。
- 日期字段：支持 Excel 日期数字、`2026-07-31`、`2026/07/31`、`2026年7月31日`。
- 完成率：如果能通过目标和完成计算，则优先计算；否则使用表格中的完成率。
- 状态：
  - 完成率 >= 时间进度：良好。
  - 完成率 >= 时间进度 - 10：预警。
  - 低于以上范围：滞后。
- 颜色：
  - 完成率 >= 60：绿色。
  - 完成率 >= 35：黄色。
  - 完成率 < 35：红色。

## 14. 维护建议

日常只维护这些表：

- `门店明细`
- `教练档案`
- `教练门店关系`
- `体验课流水`
- `续课流水`
- `双城经营`
- `六部门OKR`
- `六项目OKR`
- `转化漏斗`
- `经营重点`

尽量不要手动维护：

- 教练排名汇总
- 城区分布汇总
- 城市月/周/昨日完成
- 城市转化率

这些已由服务端自动计算，人工维护容易导致口径不一致。

## 15. 交付文件说明

核心文件：

- `index.html`：页面入口。
- `app.js`：看板交互、渲染、30 秒自动刷新、本地 Excel 上传、PNG/PDF 导出。
- `styles.css`：视觉样式。
- `data.json`：静态示例数据。
- `api/dingtalk-data.js`：Vercel 服务端钉钉同步接口。
- `vercel.json`：Vercel 函数配置。
- `package.json`：项目配置。
- `vendor/`：前端导出和 Excel 解析依赖。
- `xiaotie-logo.png`：Logo。
- `小铁台球经营仓数据模板.xlsx`：钉钉同步模板。
- `经营看板数据口径与同步说明.md`：本说明文档。

部署方式：

1. 将文件上传到 GitHub 仓库根目录。
2. Vercel 连接该仓库。
3. 在 Vercel Project Settings > Environment Variables 填写钉钉或后台环境变量。
4. 重新部署。
5. 打开线上地址，点击“刷新数据”验证同步。
