# 小铁台球教培经营看板数据口径与同步说明

更新时间：2026-08-03  
适用版本：钉钉稳定同步版 `2026-08-03-stable-dingtalk-partial-sync`

## 1. 本次修复重点

本版本专门修复钉钉同步反复 HTTP 500/503 的问题。

- 删除线上同步清单里的历史 Sheet：`个人OKR`、`教练经营`、`城区分布`。
- 首页不再展示“教练经营排名”。
- 明细大表不再一次性读取大范围，改为小分段读取。
- 单个 Sheet 或单个分段遇到钉钉 HTTP 503 时，不再让整张看板 HTTP 500。
- API 会返回已经读到的真实数据，并在 `meta.syncWarnings` 里记录哪些 Sheet/分段没读到。
- 前端顶部只显示简洁状态，例如“部分成功（3个分段跳过）”，不再铺满一整屏报错。

## 2. 当前看板页面

1. 城市经营中心
   - 深圳经营中心
   - 广州经营中心
   - 城市业绩、用户端、教练端、门店端、城区分布、经营重点提醒

2. 总部运营中心 OKR
   - 公司 KR
   - 六部门 OKR
   - 流量到转化漏斗
   - 总部运营提醒

3. 项目进度
   - 六项目 OKR
   - 目标值、完成值、完成率、风险、具体动作

## 3. 当前读取的钉钉 Sheet

正式同步只读取这些 Sheet：

- `基础配置`
- `双城经营`
- `公司KR`
- `六部门OKR`
- `六项目OKR`
- `门店明细`
- `教练档案`
- `教练门店关系`
- `体验课流水`
- `续课流水`
- `转化漏斗`
- `经营重点`

兼容别名：

- `续课流水` 可用 `正课流水`、`正课订单`、`正课数据`、`续课数据` 替代。
- `六项目OKR` 可用 `项目进度`、`六项目`、`项目OKR` 替代。
- `六部门OKR` 可用 `总部运营中心OKR`、`部门OKR`、`六部门` 替代。
- `经营重点` 可用 `运营提醒`、`经营提醒`、`风险预警` 替代。

模板已清理为当前同步需要的 Sheet；线上也以以上同步清单为准。

## 4. Vercel 环境变量

建议保留：

```env
DINGTALK_SYNC=true
DINGTALK_APP_KEY=钉钉应用AppKey
DINGTALK_APP_SECRET=钉钉应用AppSecret
DINGTALK_WORKBOOK_ID=NpQlK57dm3yvDqDv
DINGTALK_OPERATOR_ID=操作者OperatorId
DINGTALK_RANGE=A1:Z80
DINGTALK_DETAIL_RANGE=A1:Z700
DINGTALK_CONCURRENCY=1
DINGTALK_CHUNK_ROWS=120
DINGTALK_MIN_CHUNK_ROWS=40
DINGTALK_HTTP_TIMEOUT_MS=9000
DINGTALK_RETRY_ATTEMPTS=3
```

建议删除或不要设置：

```env
DINGTALK_SHEETS
DINGTALK_ENTITY_SOURCE
DINGTALK_DOC_URL
DINGTALK_USE_ENV_SHEETS
```

说明：

- `DINGTALK_WORKBOOK_ID` 当前建议使用 `NpQlK57dm3yvDqDv`。
- `DINGTALK_CONCURRENCY=1` 是为了降低钉钉 503 概率。
- `DINGTALK_DETAIL_RANGE=A1:Z700` 已覆盖当前深圳/广州教练和门店量级，同时比 `A1:Z1000` 更稳。
- 如果未来数据行超过 700 行，可以先调到 `A1:Z900`，但建议同步观察是否又出现 503。
- 不要设置 `DINGTALK_USE_ENV_SHEETS=true`，否则可能重新启用旧 sheetId。

## 5. 同步逻辑

请求路径：

```text
前端页面 -> /api/dingtalk-data -> Vercel Serverless Function -> 钉钉表格 API
```

读取方式：

- 普通配置 Sheet：默认 `A1:Z80`。
- `双城经营`：默认 `A1:Z20`。
- 明细 Sheet：默认 `A1:Z700`。
- 明细 Sheet 会拆成多个 120 行小块读取。
- 如果某个小块 HTTP 503，会继续拆小到 40 行；仍失败则跳过该块并记录 warning。

返回策略：

- 只要 token/workbook 权限没整体失败，API 尽量返回 HTTP 200。
- 部分 Sheet/分段失败时，返回真实已读数据 + `syncWarnings`。
- 只有 accessToken、workbookId、operatorId、环境变量等整体性错误，才返回 HTTP 500。

## 6. 时间口径

系统按北京时间 `Asia/Shanghai` 计算。

- 本月：当前自然月。
- 昨日：当前日期前一天。
- 本周：最近 7 天。

今天是 2026-08-03 时：

- 本月为 2026-08-01 至 2026-08-31。
- 昨日为 2026-08-02。

门店新增按 `入驻日期/入驻时间/创建时间` 判断。  
教练新增按 `入职日期/入职时间/创建时间` 判断。  
订单和流水按 `日期/下单日期/支付日期/创建时间` 判断。

## 7. 城市经营中心口径

业绩：

```text
月度完成 = 本月体验课流水金额 + 本月续课/正课流水金额
完成率 = 月度完成 / 月度目标
周完成 = 最近 7 天体验课流水金额 + 最近 7 天续课/正课流水金额
周完成率 = 周完成 / 周目标
昨日完成 = 昨日体验课流水金额 + 昨日续课/正课流水金额
```

用户端：

```text
月度体验课数 = 本月体验课流水.下单数求和
月度成交数 = 本月体验课流水.转化数/成交数求和
月度转化率 = 月度成交数 / 月度体验课数
月度续课数 = 本月续课/正课流水.续约数/续课数求和
```

教练端：

```text
总教练数 = 教练档案中该城市在职教练数
本月新增教练 = 入职日期在本月的教练
昨日新增教练 = 入职日期在昨日的教练
```

门店端：

```text
入驻门店数 = 门店明细中该城市门店总数
付费入驻 = 入驻类型包含“付费” 或 是否付费为真/是/1
免费入驻 = 入驻门店数 - 付费入驻
本月新签 = 入驻日期在本月的门店
昨日新增 = 入驻日期在昨日的门店
```

城区分布：

```text
按 城市 + 区域 聚合教练档案和门店明细
展示每个区的教练数、教练名字、门店数、门店名字
```

一个教练服务多个门店：

```text
优先读取 教练门店关系
同一教练ID对应多个门店时合并展示
```

## 8. 常见错误判断

`HTTP 500`：

- 多数是环境变量、钉钉权限、workbookId/operatorId 错误。
- 直接打开 `/api/dingtalk-data` 看 JSON message。

`sheetId=s14`：

- 说明 Vercel 还在跑旧代码或旧 `DINGTALK_SHEETS`。
- 处理：删除 `DINGTALK_SHEETS`，重新上传本包并部署。

`uuid not exist`：

- workbookId 或 sheetId 不匹配当前钉钉文档。
- 当前建议 `DINGTALK_WORKBOOK_ID=NpQlK57dm3yvDqDv`。

`HTTP 503 ServiceUnavailable`：

- 钉钉服务临时失败，或者一次读取范围/并发太大。
- 本版本已改为分段读取和跳过失败分段。
- 持续出现时优先降低 `DINGTALK_DETAIL_RANGE` 或 `DINGTALK_CHUNK_ROWS`。

## 9. 部署后验证

1. 上传本压缩包内文件到 GitHub 根目录。
2. 等 Vercel 自动部署完成。
3. 打开 `https://xt-bi-kanban.vercel.app/api/dingtalk-data`。
4. 确认返回 JSON 里 `meta.apiVersion` 为：

```text
2026-08-03-stable-dingtalk-partial-sync
```

5. 再打开首页，查看顶部同步状态。

如果顶部仍显示“核心经营数据未完整同步，已拒绝返回半成品数据”，说明线上跑的不是本版本，需要重新覆盖 GitHub 并重新部署。
